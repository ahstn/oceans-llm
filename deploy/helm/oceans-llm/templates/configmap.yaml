apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "oceans-llm.configMapName" . }}
  labels:
    {{- include "oceans-llm.labels" . | nindent 4 }}
data:
  gateway.yaml: |
    {{- toYaml .Values.gateway.config | nindent 4 }}
