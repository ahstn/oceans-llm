database:
  mode: external

secrets:
  inline:
    POSTGRES_URL: postgres://oceans:replace-me@postgres.example.internal:5432/oceans_llm
    GATEWAY_IDENTITY_TOKEN_SECRET: replace-with-32-byte-random-secret
    OPENAI_API_KEY: replace-me

migrations:
  enabled: true
